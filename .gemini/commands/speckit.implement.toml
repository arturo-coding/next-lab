description = "Execute the implementation plan by processing and executing all tasks defined in tasks.md"

[outline]
step1 = "Run `.specify/scripts/bash/check-prerequisites.sh --json --require-tasks --include-tasks` from repo root."
step2 = "Check checklists status (if FEATURE_DIR/checklists/ exists)."
step3 = "Load and analyze the implementation context (tasks.md, plan.md, etc.)."
step4 = "Project Setup Verification: Create/verify ignore files."
step5 = "Parse tasks.md structure and execute implementation following the task plan."
step6 = "Completion validation: Verify all required tasks are completed."
